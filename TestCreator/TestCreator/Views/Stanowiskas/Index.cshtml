@model IEnumerable<TestCreator.Data.Stanowiska>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.nazwa_stanowiska)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.is_visible)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.nazwa_stanowiska)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.is_visible)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.id_stanowisko }) |
            @Html.ActionLink("Details", "Details", new { id=item.id_stanowisko }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.id_stanowisko })
        </td>
    </tr>
}

</table>
